# fastAPI-blog-practice
Simple REST API for an unexistent Blog website, just for practice, using **FastAPI** and **SQLAlchemy**

### Dependencies
```bash
pip install fastapi uvicorn sqlalchemy python-jose[cryptography] passlib pydantic
```
